Необходимые библиотеки и питон

Python 3.8
pip install opencv-python
pip install scikit-learn
pip install tensorflow
pip install pyqt5-tools
pip install PyQt5
pip install pandas
pip install Pillow
pip install pyinstaller
pip install pathlib

Сборка экзешника:
    0. Если ранее осуществлялась сборка, удалить dist/mainUI
    1. Выполниь команду: pyinstaller mainUi.py
    2. Скопировать ProjectData в dist/mainUI
    3. Готовый экзешник: dist/mainUI/mainUI.exe
    4. В папке dist/mainUI - находятся все необходимые библиотеки

Конвертация ui в py:
    pyuic5 UI/MainWindow.ui -o UI/MainWindow.py

Настройки классификаторов:
    ProjectData/SystemFiles/params.json


